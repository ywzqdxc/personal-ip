# Personal IP Site — 本地开发环境搭建说明

> 仓库地址：https://github.com/ywzqdxc/personal-ip
> 技术栈：RuoYi-Vue-Pro（Java 后端 + 统一内容管理）+ Next.js（个人主页前台）+ Vue 3（管理后台）

---

## 一、拉取项目

```bash
# 克隆整个仓库（包含后端 + 管理后台 + 前台三个子项目）
git clone https://github.com/ywzqdxc/personal-ip.git
cd personal-ip
```

克隆完成后，目录结构如下：

```
personal-ip/
├── personal-ip-backend/        ← RuoYi-Vue-Pro 后端（Spring Boot 3.5.9 + Java 17）
│   └── yudao-module-ip/        ← 自定义 IP 内容管理模块
├── personal-ip-admin/          ← 管理后台前端（Vue 3 + Element Plus）
├── personal-ip-frontend/       ← 个人主页前台（Next.js 14 + React 19 + TypeScript）
├── design-requirements.md      ← 前端设计需求文档（AI 接手先读这个）
└── 环境搭建说明.md              ← 本文件
```

---

## 二、前置要求

开始之前，确认以下工具已安装并在 PATH 中可用：

| 工具 | 版本要求 | 验证命令 |
|------|----------|----------|
| JDK | 17 | `java -version` |
| Maven | 3.8+ | `mvn -v` |
| Node.js | 18+ | `node -v` |
| pnpm | 8+ | `pnpm -v` |
| MySQL | 8.x | `mysql --version` |
| Redis | 任意 | `redis-server --version` |
| Git | 任意 | `git --version` |

> **Redis 未安装？** 下载 Windows 版：
> https://github.com/tporadowski/redis/releases
> 下载 `Redis-x64-5.0.14.1.zip`，解压到任意目录，运行 `redis-server.exe` 即可。

> **pnpm 未安装？** 执行：`npm install -g pnpm`

---

## 三、RuoYi-Vue-Pro 后端

> 后端源码已在仓库内（`personal-ip-backend/`），无需单独克隆。

### 3.1 创建数据库

用任意 MySQL 客户端（Navicat、DataGrip、命令行均可）执行：

```sql
CREATE DATABASE `ruoyi-vue-pro`
    DEFAULT CHARACTER SET utf8mb4
    DEFAULT COLLATE utf8mb4_unicode_ci;

CREATE DATABASE `ruoyi-vue-pro-job`
    DEFAULT CHARACTER SET utf8mb4
    DEFAULT COLLATE utf8mb4_unicode_ci;
```

### 3.2 导入初始 SQL

SQL 文件在克隆目录的 `sql/mysql/` 下，**按顺序执行**：

```
personal-ip-backend/sql/mysql/ruoyi-vue-pro.sql      → 导入到 ruoyi-vue-pro 库
personal-ip-backend/sql/mysql/ruoyi-vue-pro-job.sql  → 导入到 ruoyi-vue-pro-job 库
```

命令行方式：

```bash
mysql -uroot -p ruoyi-vue-pro     < personal-ip-backend/sql/mysql/ruoyi-vue-pro.sql
mysql -uroot -p ruoyi-vue-pro-job < personal-ip-backend/sql/mysql/ruoyi-vue-pro-job.sql
```

### 3.3 配置本地参数

找到以下文件并创建/编辑（IDEA 会优先加载 local profile）：

```
personal-ip-backend/yudao-server/src/main/resources/application-local.yaml
```

如果文件不存在就新建，内容如下（**只需改密码**）：

```yaml
spring:
  datasource:
    dynamic:
      datasource:
        master:
          url: jdbc:mysql://127.0.0.1:3306/ruoyi-vue-pro?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&nullCatalogMeansCurrent=true
          username: root
          password: 你的MySQL密码     # ← 只需改这里
        quartz:
          url: jdbc:mysql://127.0.0.1:3306/ruoyi-vue-pro-job?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
          username: root
          password: 你的MySQL密码     # ← 和上面保持一致
  redis:
    host: 127.0.0.1
    port: 6379
    database: 0

logging:
  level:
    cn.iocoder.yudao: debug
```

### 3.4 IDEA 中启动后端

1. 用 IntelliJ IDEA 打开 `personal-ip-backend/` 目录
2. 等待 Maven 依赖下载完成
3. 找到启动类：`yudao-server/src/main/java/.../YudaoServerApplication.java`
4. 编辑运行配置，在 **Active profiles** 中填入 `local`
5. 运行即可

启动成功后访问：
- API 地址：http://localhost:48080
- Swagger 文档：http://localhost:48080/doc.html

### 3.5 启动管理后台前端

```bash
cd D:\UserData\idea\personal-ip-admin

pnpm install
pnpm run dev
```

访问：http://localhost:80
默认账号：`admin` / `admin123`

---

## 四、个人主页前台（Next.js）

### 4.1 安装依赖 & 启动

```bash
cd personal-ip-frontend

pnpm install
pnpm dev
```

访问：http://localhost:3000

### 4.2 关键页面路由

| 路由 | 页面 | 状态 |
|------|------|------|
| `/` | 首页 Hero | ⚠️ 框架存在，待完善 |
| `/about` | 关于我 Bento Grid Hub | ✅ 完成 |
| `/about/hobby` | Hobby 子页 | ✅ 完成 |
| `/about/hobby/running` | Running 详情页 | ✅ 完成 |
| `/about/hobby/table-tennis` | Table Tennis 详情页 | ✅ 完成 |
| `/about/story` | 我的故事 | ⬜ 待实现 |
| `/about/tech` | 技术栈 | ⬜ 待实现 |
| `/about/career` | 职业经历 | ⬜ 待实现 |
| `/travel` | 旅行年份浮动卡片 | ✅ 完成 |
| `/travel/[year]` | 某年旅行列表 | ✅ 完成 |
| `/travel/[year]/[tripId]` | 旅行日记详情 | ✅ 完成 |
| `/projects` | 项目展示 | ⬜ 待实现 |
| `/thoughts` | 随想碎片 | ⬜ 待实现 |
| `/blog` | 博客文章 | ⬜ 待实现 |

### 4.3 环境变量

在 `personal-ip-frontend/` 下创建 `.env.local`（如不存在）：

```env
# 后端 API 地址
NEXT_PUBLIC_API_BASE=http://localhost:48080
```

---

## 五、启动顺序

每次开发时，按以下顺序启动服务：

```
1. 启动 Redis        →  运行 redis-server.exe
2. 确认 MySQL 在运行 →  检查服务或 Navicat 连接
3. 启动 RuoYi 后端  →  IDEA 运行 YudaoServerApplication（profile=local）
4. 启动管理前端      →  cd personal-ip-admin && pnpm dev
5. 启动前台页面      →  cd personal-ip-frontend && pnpm dev
```

用以下命令快速验证各服务是否在线：

```bash
# Windows PowerShell
@(6379,3306,48080,80,3000) | ForEach-Object {
    $ok = (New-Object Net.Sockets.TcpClient).ConnectAsync("127.0.0.1",$_).Wait(500)
    Write-Host "Port $_`: $(if($ok){'UP'}else{'DOWN'})"
}
```

---

## 六、服务地址汇总

| 服务 | 地址 | 账号 |
|------|------|------|
| RuoYi 后端 API | http://localhost:48080 | — |
| Swagger 文档 | http://localhost:48080/doc.html | — |
| 管理后台 | http://localhost:80 | admin / admin123 |
| 个人主页前台 | http://localhost:3000 | — |

---

## 七、常见问题

**Q: Maven 下载依赖很慢？**
在 Maven 全局 `settings.xml` 中加阿里云镜像：
```xml
<mirror>
  <id>aliyun</id>
  <mirrorOf>*</mirrorOf>
  <url>https://maven.aliyun.com/repository/public</url>
</mirror>
```

**Q: RuoYi 启动报 Redis 连接失败？**
确认 `redis-server.exe` 已运行，默认端口 6379 未被占用。

**Q: RuoYi 启动报数据库连接失败？**
检查 `application-local.yaml` 中 MySQL 密码是否正确，数据库 `ruoyi-vue-pro` 是否已创建并导入 SQL。

**Q: 管理前端 pnpm install 报错？**
尝试：`pnpm install --legacy-peer-deps`，或确认 Node.js 版本为 18.x。

**Q: 端口冲突？**
RuoYi 默认 48080，可在 `application-local.yaml` → `server.port` 修改。
